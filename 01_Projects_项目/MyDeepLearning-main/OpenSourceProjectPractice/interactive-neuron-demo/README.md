# Interactive Neuron Demo

Open `index.html` in a browser.

This demo intentionally shows only one neuron:

```text
z = w1*x1 + w2*x2 + b
a = sigmoid(z)
```

Use it before studying multi-layer neural networks. It is meant to make input nodes, connection weights, bias, sigmoid activation, and binary decision thresholds visible.
